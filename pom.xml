<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.accelhack.monica</groupId>
  <artifactId>monica-java-sdk</artifactId>
  <version>0.1.1-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>MONICA Java SDK</name>
  <description>Java integrations for MONICA error monitoring</description>
  <url>https://github.com/Accel-Hack/monica-sdk-java</url>

  <developers>
    <developer>
      <name>Accel Hack</name>
      <organization>Accel Hack</organization>
      <organizationUrl>https://github.com/Accel-Hack</organizationUrl>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:https://github.com/Accel-Hack/monica-sdk-java.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/Accel-Hack/monica-sdk-java.git</developerConnection>
    <url>https://github.com/Accel-Hack/monica-sdk-java</url>
    <tag>HEAD</tag>
  </scm>

  <!--
    公開先は GitHub Packages。registry が repository と 1 対 1 なので URL に owner/repo が入る。
    module ごとに URL を変えず、parent のこれを全 module が継承する。
    Maven registry は public な package でも匿名取得を許さないので、利用側は手元なら
    read:packages を持つ token を settings.xml に、CI なら GITHUB_TOKEN を渡す必要がある
    （README の「利用側の設定」を参照）。
    id は setup-java の server-id と揃える。ずれると deploy が 401 になる。
  -->
  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Packages</name>
      <url>https://maven.pkg.github.com/Accel-Hack/monica-sdk-java</url>
    </repository>
    <snapshotRepository>
      <id>github</id>
      <name>GitHub Packages</name>
      <url>https://maven.pkg.github.com/Accel-Hack/monica-sdk-java</url>
    </snapshotRepository>
  </distributionManagement>

  <modules>
    <module>core</module>
    <module>logback</module>
    <module>spring-boot2-starter</module>
  </modules>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>11</maven.compiler.release>
    <jackson.version>2.13.5</jackson.version>
    <junit.version>5.9.3</junit.version>
    <logback.version>1.2.13</logback.version>
    <spring-boot.version>2.6.15</spring-boot.version>
    <!--
      monica-android (Accel-Hack/monica-sdk-android) ships monica-core inside every APK, so
      core is still held to the JDK API surface of the lowest Android release it supports.
      javac with release 11 happily accepts List.of() or String.strip(), which only exist
      on API 30 / 35; animal-sniffer fails the build for what API 26 cannot resolve.
    -->
    <android-api.signature>gummy-bears-api-26</android-api.signature>
    <android-api.signature.version>0.12.0</android-api.signature.version>
    <project.build.outputTimestamp>2026-08-30T00:00:00Z</project.build.outputTimestamp>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-classic</artifactId>
        <version>${logback.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.4.0</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <phase>package</phase>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>3.12.0</version>
        <executions>
          <execution>
            <id>attach-javadocs</id>
            <phase>package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <release>${maven.compiler.release}</release>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.2.5</version>
          <configuration>
            <useModulePath>false</useModulePath>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>animal-sniffer-maven-plugin</artifactId>
          <version>1.24</version>
          <configuration>
            <signature>
              <groupId>com.toasttab.android</groupId>
              <artifactId>${android-api.signature}</artifactId>
              <version>${android-api.signature.version}</version>
            </signature>
          </configuration>
          <executions>
            <execution>
              <id>android-api-level</id>
              <phase>process-classes</phase>
              <goals>
                <goal>check</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

</project>
